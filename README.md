# UNILAG CBT Web

React frontend for the free UNILAG Post-UTME CBT practice portal.

```powershell
npm install
Copy-Item .env.example .env
npm run dev
```

The FastAPI backend should be running at `http://localhost:8000`.

